
# Task: Desktop UI (Electron + React)

## Goal
Unified dashboard: inbox, search, graph, sync status, rule builder.

## Sub‑tasks
- IPC secure bridge; context isolation on.
- Editor: MD + YAML frontmatter.
- Graph view (vis-network) and rule builder UI.

## Micro‑tasks
- `SearchView` calling FTS5 endpoint with ranking.
- `RuleForm` → JSON rule; validate and save.
